name: Reuters Energy Automation

on:
  schedule:
    - cron: "0 */2 * * *"
  workflow_dispatch:

jobs:
  run-reuters-energy-automation:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger LE BRIEF Reuters automation
        run: |
          curl --fail --show-error --silent \
            "https://www.lebrief.energy/api/cron/reuters-energy"
